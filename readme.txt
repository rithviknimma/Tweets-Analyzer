Tweets-Analyzer:

Handles JSON arrays from a Twitter API to analyze and filter tweets by username, keywords and timespan.

Infers social networks of people by analyzing tweets for cliques and mentions of other users.


Compile: javac -cp lib/json-20190722.jar src/assignment4/*.java -d bin

Run: java -cp bin:lib/json-20190722.jar assignment4.Main

